# WhatsApp Templates (local)

Templates and images are stored **in code**.

- Edit message text in `src/templates/messages/index.ts`
- Drop reference images into `src/templates/images/` and import them in `messages/index.ts`

Managers pick a template when sending WhatsApp from the lead dialog.
